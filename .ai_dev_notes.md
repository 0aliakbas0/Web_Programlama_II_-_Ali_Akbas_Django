# Projede Kaldığımız Yer ve Yapılacaklar (Modifikasyon Planı)

## Mevcut Durum (Ne Yapıldı?)
- Django resmi dokümantasyonundaki "8 Parçalı Eğitimin" orijinal ve değiştirilmemiş hali klasöre eklendi.
- Veritabanı başlatıldı (`Question` ve `Choice` modelleri).
- `git config` (akbasali049@gmail.com) eklendi ve tüm orijinal dosyalar `İlk 8 tutorial'in orjinal hali` mesajıyla GitHub'a ana dal (main) olarak itildi (pushlandı).
- Kullanıcıya ait README.md dosyası oluşturularak "Projenin nasıl çalıştırılacağı" ve "Gelecek Planları" yazılarak eklendi ve GitHub'a başarıyla gönderildi.
- Kullanıcının izni olmadan yanlışlıkla kurulan Bootstrap ve yeni sayfa yönlendirmeleri kodları TAMAMEN GERİ ALINDI (`git reset --hard HEAD`), ve README'de "Her şey Django ile yapılacak" ifadesi güncellendi.

## Gelecek Planı (Geri Dönüldüğünde Ne Yapılacak?)
Kullanıcı geri döndüğünde, Mesut Hoca'nın vize/final beklentilerine uygun olarak **Django ile aşağıdaki modifikasyonlar (Değişiklik ve Geliştirmeler)** kodlanacaktır. 
*(ÖNEMLİ: React, Vue veya harici backend API'leri kullanılmayacak. Tüm kodlama Django'nun MVC - MVT, Views, ve Template yapısı içerisinde çözülecek.)*

1. **Ana Sayfa Yönlendirmesi (URL Yönlendirme):**
   - Kullanıcı `127.0.0.1:8000/` adresine girdiğinde `urls.py` dosyasından `RedirectView` ile otomatik olarak `/polls/` ana sistemine (Oylama Sayfasına) aktarılacak.
   
2. **Kavram Değişiklikleri (Modeller - Viewler):**
   - "Polls" anket mantığı, "Teknofest Proje Fikri Oylaması" konseptine dönüştürülecek. Başlıklar ve HTML içi metinler güncellenecek.
   
3. **Tasarım Güncellemeleri (Django Templates İçerisinde Bootstrap):**
   - `/polls/templates/` altındaki Index, Detail ve Results HTML'lerine Bootstrap `cdn` eklenecek.
   - Düz `<ul>` (Liste) yapıları yerine, şık **Bootstrap Cards (Kartlar)** ve List Grouplara dönüştürülecek.
   - Sonuçlar `/results/` ekranında; oy oranları düz rakam olarak değil, `views.py` içinde küçük bir hesaplama ile (Seçilen Oy / Toplam Oy * 100) Bootstrap'in **Progress Bar (İlerleme Çubuğu)** yapısıyla çok renkli hale getirilecek.

4. **Yeniliklerin Git'e Pushlanması:**
   - Modifikasyon kodlamaları her bir "Ana" adım tamamlandığında, kullanıcıya gösterilerek GitHub'a "Oylama sistemi konsepti tasarlandı" gibi ayrı ayrı anlamlı commitlerle yedeklenecek.
   
*(Not: Bu dosya sadece AI Asistanın (Benim) mevcut durumu hatırlamam içindir, repo'ya yüklenmemiştir.)*
